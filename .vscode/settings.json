{
    "sqltools.connections": [
        {
            "previewLimit": 50,
            "driver": "SQLite",
            "name": "TT",
            "database": "${workspaceFolder:TimeTracker}/database/time.db"
        }
    ]
}